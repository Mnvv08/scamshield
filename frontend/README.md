# ScamShield frontend

React + Vite dashboard for the ScamShield fraud-detection API. See the root
[README](../README.md) for full project setup and architecture.

```bash
npm install
echo "VITE_API_URL=http://localhost:8000" > .env
npm run dev
```
