// Central place to set your backend URL. Defaults to the deployed Render
// backend; falls back to localhost automatically if that's unreachable
// (useful while developing locally).
const SCAMSHIELD_CONFIG = {
  PROD_API: "https://scamshield-9ksh.onrender.com",
  DEV_API: "http://localhost:8000",
};
